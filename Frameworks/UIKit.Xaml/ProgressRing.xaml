<Grid
    x:Class="UIKit.Xaml.ProgressRing"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UIKit.Xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="Root">
    <!-- TODO: Update property name in path for IsActive binding once it has been updated in the header -->
    <ProgressRing x:Name="progressRing"
                  IsActive="{Binding ElementName=Root, Path=ProgressRing_IsActive, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"/>
</Grid>